{{> navbar}}
<div class="container mt-3">
  <h1>
    {{#if id}}
      Actualizar la historia "{{title}}" ({{id}})
    {{else}}
      Crear una nueva historia
    {{/if}}
  </h1>
  <!--
    Este formulario se usará tanto para edición como creación, este if nos dice que si viene un id, entonces será un update, si no, será un create.
    Para el action, si viene un id será un post a /stories/:id/update, si no, será un post a /stories/create.
  -->
  <form action="/stories/{{#if id}}{{id}}/update{{else}}create{{/if}}" method="POST">
    <div class="mb-3">
      <label for="title" class="form-label">Título</label>
      <input type="text" class="form-control" name="title" aria-describedby="titleHelp" required="true" value="{{title}}">
      <div id="titleHelp" class="form-text">Utiliza un título que describa tu historia</div>
    </div>
    <div class="mb-3">
      <label for="topic" class="form-label">Tópico (fauna, flora, paisaje)</label>
      <input type="text" class="form-control" name="topic" aria-describedby="topicHelp" required="true" value="{{title}}">
    </div>
    <div class="mb-3">
      <label for="content" class="form-label">Contenido</label>
      <textarea class="form-control" name="content" rows="3" aria-describedby="contentHelp">{{content}}</textarea>
      <div id="contentHelp" class="form-text">¿Donde fuiste y que fué lo que viste?, cuéntame!!</div>
    </div>
    <button type="submit" class="btn btn-primary">Enviar</button>
    </div>
  </form>
</div>